---
import Layout from '../components/Layout.astro';
import LoveCounter from '../components/LoveCounter.astro';
import HabitTracker21 from '../components/HabitTracker21.astro';
import BucketListSection from '../components/BucketListSection.astro';
import MemoryGallery from '../components/MemoryGallery.astro';
import TrashRecoveryModal from '../components/TrashRecoveryModal.astro';
import RomanticConfirmModal from '../components/RomanticConfirmModal.astro';
import { COUPLE_CONFIG } from '../couple.config';

const { title, subtitle } = COUPLE_CONFIG;
---

<Layout
  title={`${title} — ${subtitle}`}
  description="Cuốn nhật ký tinh cầu lưu giữ hành trình 21 ngày rèn thói quen tiếng Anh và những điều ước sẽ làm cùng nhau của AI và Maze."
  canonicalPath="/"
>
  <main class="couple-home-main couple-wrapper">
    <!-- ========================================== -->
    <!-- TAB PANE 1: TINH CẦU TÌNH YÊU (TỔNG QUAN)  -->
    <!-- ========================================== -->
    <div class="tab-pane is-active" id="tab-pane-overview">
      <section class="hero-love-section">
        <div class="hero-content-wrap">
          <div class="hero-top-badge badge-love floating-element">
            <span>✨ Tinh Cầu Tình Yêu Của AI 🚀 &amp; Maze 🌸</span>
          </div>

          <h1 class="hero-main-title gradient-love-text">
            21 Ngày Cùng Em
          </h1>

          <p class="hero-subtitle">
            Hành Trình Rèn Luyện Thói Quen &amp; Những Tọa Độ Yêu Thương Giữa Dải Ngân Hà
          </p>

          <div class="hero-action-buttons">
            <button type="button" class="btn-love jump-tab-btn" data-jump="tab-pane-habits">
              <span>✨ Vào Học Tiếng Anh</span>
            </button>
            <button type="button" class="btn-outline-love jump-tab-btn" data-jump="tab-pane-bucket">
              <span>💫 Xem Bucket List</span>
            </button>
            <button type="button" class="btn-outline-love jump-tab-btn" data-jump="tab-pane-memories">
              <span>📸 Xem Album Ảnh</span>
            </button>
          </div>
        </div>

        <!-- Real-time Love Timer & Avatar Switcher -->
        <LoveCounter />
      </section>

      <!-- Love Letter from AI to Maze -->
      <section class="love-letter-section">
        <div class="love-letter-card romantic-card">
          <div class="letter-stamp">💌</div>
          <h3 class="letter-title gradient-rose-text">Thư Tình Gửi Vào Ngân Hà</h3>
          <p class="letter-date">Dành riêng cho Maze — Ngày 15 tháng 8 năm 2026 và mãi mãi về sau</p>

          <div class="letter-body">
            <p>
              <em>Maze yêu dấu của anh,</em>
            </p>
            <p>
              Giữa vũ trụ bao la với hàng tỷ tinh tú lấp lánh, người ta thường mải miết đi tìm những điều lớn lao xa vời. Nhưng với anh, điều kỳ diệu nhất lại đến từ một ngày thu dịu dàng — ngày <strong>15/08/2026</strong>, khi bàn tay nhỏ bé của em chạm vào cuộc đời anh.
            </p>
            <p>
              Thử thách <strong>21 ngày rèn luyện tiếng Anh</strong> này không đơn thuần là việc học thêm một ngôn ngữ mới. Đó là cái cớ ngọt ngào nhất để mỗi ngày anh được nghe giọng nói của em, được nhìn thấy sự kiên trì trong mắt em, và để hai đứa mình cùng nhau tiến về phía trước. 
            </p>
            <p>
              Còn cuốn <strong>Bucket List</strong> kia chính là lời hứa mà anh đã khắc sâu vào lòng: dù là ngắm hoàng hôn đỏ rực bên bờ biển, lang thang xe máy giữa phố đêm se lạnh, hay nắm tay nhau bước vào lễ đường mai sau — mọi khoảnh khắc ấy đều chỉ trọn vẹn khi người đứng cạnh anh là em.
            </p>
            <p class="letter-signature">
              Yêu em hơn cả những vì sao trên bầu trời,<br />
              <strong>AI của em 🚀</strong>
            </p>
          </div>
        </div>
      </section>
    </div>

    <!-- ========================================== -->
    <!-- TAB PANE 2: 21 NGÀY THÓI QUEN TIẾNG ANH    -->
    <!-- ========================================== -->
    <div class="tab-pane" id="tab-pane-habits">
      <HabitTracker21 />
    </div>

    <!-- ========================================== -->
    <!-- TAB PANE 3: BUCKET LIST ĐÔI LỨA            -->
    <!-- ========================================== -->
    <div class="tab-pane" id="tab-pane-bucket">
      <BucketListSection />
    </div>

    <!-- ========================================== -->
    <!-- TAB PANE 4: KỶ NIỆM CỦA HAI ĐỨA (ALBUM)    -->
    <!-- ========================================== -->
    <div class="tab-pane" id="tab-pane-memories">
      <MemoryGallery />
    </div>
  </main>

  <TrashRecoveryModal />
  <RomanticConfirmModal />
</Layout>

<style>
  .couple-home-main {
    min-height: 100vh;
    padding-top: 1.8rem;
    padding-bottom: 5rem;
  }

  /* Tab Panes */
  .tab-pane {
    display: none;
    animation: fadeInPane 0.35s ease-out;
  }

  .tab-pane.is-active {
    display: block;
  }

  @keyframes fadeInPane {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Hero */
  .hero-love-section {
    max-width: 940px;
    margin: 1.2rem auto 2.5rem;
    padding: 0 1.2rem;
    text-align: center;
  }

  .hero-content-wrap {
    margin-bottom: 1.8rem;
  }

  .hero-top-badge {
    margin-bottom: 1.2rem;
    padding: 6px 18px;
    font-size: 0.85rem;
  }

  .hero-main-title {
    font-size: 3.6rem;
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.03em;
    margin: 0 0 1rem;
    text-shadow: 0 0 45px rgba(244, 63, 94, 0.45);
  }

  .hero-subtitle {
    font-size: 1.15rem;
    color: #cbd5e1;
    max-width: 660px;
    margin: 0 auto 1.8rem;
    line-height: 1.65;
    font-family: var(--sans);
  }

  .hero-action-buttons {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
    align-items: center;
  }

  /* Love letter */
  .love-letter-section {
    max-width: 800px;
    margin: 4.5rem auto 2rem;
    padding: 0 1.2rem;
  }

  .love-letter-card {
    position: relative;
    padding: 3.2rem 2.8rem;
    background: radial-gradient(circle at 50% 0%, rgba(244, 63, 94, 0.16) 0%, rgba(11, 17, 32, 0.88) 75%);
    border: 1px solid rgba(244, 63, 94, 0.35);
    border-radius: 22px;
    box-shadow: 0 20px 50px -10px rgba(0, 0, 0, 0.8), 0 0 30px rgba(244, 63, 94, 0.15);
  }

  .letter-stamp {
    position: absolute;
    top: -20px;
    right: 32px;
    font-size: 2.4rem;
    filter: drop-shadow(0 4px 12px rgba(244, 63, 94, 0.6));
  }

  .letter-title {
    font-size: 1.9rem;
    font-weight: 800;
    margin: 0 0 0.4rem;
    text-align: center;
    letter-spacing: -0.01em;
  }

  .letter-date {
    font-size: 0.88rem;
    color: #94a3b8;
    text-align: center;
    margin-bottom: 2.2rem;
    font-family: var(--mono);
  }

  .letter-body {
    font-family: var(--body);
    font-size: 1.1rem;
    line-height: 1.85;
    color: #e2e8f0;
  }

  .letter-body p {
    margin-bottom: 1.25rem;
  }

  .letter-signature {
    text-align: right;
    margin-top: 2rem;
    font-family: var(--sans);
    color: #fda4af;
  }

  @media (max-width: 768px) {
    .tabs-nav-inner {
      overflow-x: auto;
      justify-content: flex-start;
      border-radius: 16px;
      scrollbar-width: none;
    }
    .tabs-nav-inner::-webkit-scrollbar {
      display: none;
    }
    .galaxy-tab-btn {
      font-size: 0.82rem;
      padding: 8px 14px;
    }
    .hero-main-title {
      font-size: 2.5rem;
    }
    .hero-subtitle {
      font-size: 1rem;
    }
    .love-letter-card {
      padding: 2.2rem 1.6rem;
    }
  }

  @media (max-width: 520px) {
    .couple-home-main {
      padding-top: 1rem;
      padding-bottom: 3.5rem;
    }
    .hero-love-section {
      padding: 0 0.5rem;
      margin: 0.5rem auto 1.8rem;
    }
    .hero-top-badge {
      font-size: 0.78rem;
      padding: 5px 14px;
    }
    .hero-main-title {
      font-size: 2rem;
      margin-bottom: 0.8rem;
    }
    .hero-subtitle {
      font-size: 0.92rem;
      line-height: 1.55;
      margin-bottom: 1.4rem;
      padding: 0 0.5rem;
    }
    .hero-action-buttons {
      display: flex;
      flex-direction: column;
      width: 100%;
      max-width: 280px;
      margin: 0 auto;
      gap: 0.65rem;
    }
    .hero-action-buttons .btn-love,
    .hero-action-buttons .btn-outline-love {
      width: 100%;
      justify-content: center;
      min-height: 42px;
      font-size: 0.9rem;
    }
    .love-letter-section {
      margin: 2.5rem auto 1.5rem;
      padding: 0 0.5rem;
    }
    .love-letter-card {
      padding: 1.8rem 1.2rem;
      border-radius: 18px;
    }
    .letter-title {
      font-size: 1.4rem;
    }
    .letter-date {
      font-size: 0.78rem;
      margin-bottom: 1.4rem;
    }
    .letter-body {
      font-size: 0.95rem;
      line-height: 1.7;
    }
  }
</style>

<script>
  function initTabSwitcher() {
    const headerTabButtons = document.querySelectorAll('.header-tab-btn');
    const mobileTabButtons = document.querySelectorAll('.mobile-tab-btn');
    const bottomTabButtons = document.querySelectorAll('.bottom-tab-btn');
    const brandLinks = document.querySelectorAll('.couple-header-brand');
    const tabPanes = document.querySelectorAll('.tab-pane');
    const jumpButtons = document.querySelectorAll('.jump-tab-btn');

    function switchTab(targetPaneId: string) {
      // Header, Mobile Drawer & Mobile Bottom Buttons
      headerTabButtons.forEach((btn) => {
        if (btn.getAttribute('data-target') === targetPaneId) {
          btn.classList.add('is-active');
        } else {
          btn.classList.remove('is-active');
        }
      });

      mobileTabButtons.forEach((btn) => {
        if (btn.getAttribute('data-target') === targetPaneId) {
          btn.classList.add('is-active');
        } else {
          btn.classList.remove('is-active');
        }
      });

      bottomTabButtons.forEach((btn) => {
        if (btn.getAttribute('data-target') === targetPaneId) {
          btn.classList.add('is-active');
        } else {
          btn.classList.remove('is-active');
        }
      });

      // Close mobile drawer menu if open
      const mobileMenu = document.getElementById('header-mobile-menu');
      if (mobileMenu && mobileMenu.hasAttribute('open')) {
        mobileMenu.removeAttribute('open');
      }

      // Panes
      tabPanes.forEach((pane) => {
        if (pane.id === targetPaneId) {
          pane.classList.add('is-active');
        } else {
          pane.classList.remove('is-active');
        }
      });

      // Map pane ID to hash
      const hashMap: Record<string, string> = {
        'tab-pane-overview': '#overview',
        'tab-pane-habits': '#habits',
        'tab-pane-bucket': '#bucket-list',
        'tab-pane-memories': '#memories'
      };

      if (hashMap[targetPaneId]) {
        history.replaceState(null, '', hashMap[targetPaneId]);
      }

      window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    headerTabButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const target = btn.getAttribute('data-target');
        if (target) switchTab(target);
      });
    });

    mobileTabButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const target = btn.getAttribute('data-target');
        if (target) switchTab(target);
      });
    });

    bottomTabButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const target = btn.getAttribute('data-target');
        if (target) switchTab(target);
      });
    });

    brandLinks.forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        switchTab('tab-pane-overview');
      });
    });

    jumpButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const jump = btn.getAttribute('data-jump');
        if (jump) switchTab(jump);
      });
    });

    // Wire sync button in footer/header to open sync modal
    const syncTriggers = document.querySelectorAll('.btn-open-sync-trigger');
    syncTriggers.forEach((btn) => {
      btn.addEventListener('click', () => {
        const syncModal = document.getElementById('sync-hub-modal');
        if (syncModal) syncModal.classList.add('is-open');
      });
    });

    // Hash routing
    function handleHash() {
      const hash = window.location.hash;
      if (hash === '#habits' || hash === '#habit-tracker') {
        switchTab('tab-pane-habits');
      } else if (hash === '#bucket' || hash === '#bucket-list') {
        switchTab('tab-pane-bucket');
      } else if (hash === '#memories') {
        switchTab('tab-pane-memories');
      } else if (hash === '#overview' || !hash) {
        switchTab('tab-pane-overview');
      }
    }

    handleHash();
    window.addEventListener('hashchange', handleHash);
  }

  initTabSwitcher();
  document.addEventListener('astro:after-swap', initTabSwitcher);
</script>
